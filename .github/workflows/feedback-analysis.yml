name: 反馈分析与自动调优

on:
  # 定时执行：每天18:30 GMT+8 = 10:30 UTC
  schedule:
    - cron: '30 10 * * *'

  # 支持手动触发
  workflow_dispatch:
    inputs:
      feedback_date:
        description: '反馈日期 (YYYYMMDD, 可选，默认为最新)'
        required: false
        type: string

# 设置时区
env:
  TZ: 'Asia/Shanghai'

jobs:
  analyze-feedback:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 拉取最新更新
        run: |
          git pull origin main

      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy

      - name: 检查反馈文件是否存在
        id: check-feedback
        run: |
          cd skills/stock_daily_recommendation/turning_feedback

          # 查找最新的反馈文件
          LATEST_FILE=$(ls -t tuning_feedback_*.csv 2>/dev/null | head -n 1)

          if [ -z "$LATEST_FILE" ]; then
            echo "found=false" >> $GITHUB_OUTPUT
            echo "⚠️  未找到反馈文件"
          else
            echo "found=true" >> $GITHUB_OUTPUT
            echo "filename=$LATEST_FILE" >> $GITHUB_OUTPUT
            echo "✅ 找到反馈文件: $LATEST_FILE"

            # 检查文件修改时间（应该在今天）
            FILE_DATE=$(date -r "$LATEST_FILE" +%Y%m%d)
            TODAY=$(date +%Y%m%d)
            echo "file_date=$FILE_DATE" >> $GITHUB_OUTPUT
            echo "today=$TODAY" >> $GITHUB_OUTPUT
          fi

      - name: 运行反馈分析
        if: steps.check-feedback.outputs.found == 'true'
        run: |
          cd skills/stock_daily_recommendation
          python run_feedback_analysis.py

      - name: 检查调优结果
        if: steps.check-feedback.outputs.found == 'true'
        id: check-tuning
        run: |
          cd skills/stock_daily_recommendation

          if [ -f "tuning_config.json" ]; then
            echo "✅ 调优配置已生成"
            echo "📊 配置内容:"
            cat tuning_config.json | python -m json.tool
            echo "tuning_generated=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️  调优配置未生成"
            echo "tuning_generated=false" >> $GITHUB_OUTPUT
          fi

      - name: 提交调优配置
        if: steps.check-tuning.outputs.tuning_generated == 'true'
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          cd skills/stock_daily_recommendation

          # 添加调优配置文件
          git add tuning_config.json

          # 检查是否有变更
          if git diff --staged --quiet; then
            echo "📝 调优配置无变更"
          else
            # 提交
            FEEDBACK_FILE="${{ steps.check-feedback.outputs.filename }}"
            git commit -m "🔧 自动调优: 基于 ${FEEDBACK_FILE} 的反馈分析

生成时间: $(date '+%Y-%m-%d %H:%M:%S %Z')
反馈文件: ${FEEDBACK_FILE}

Co-Authored-By: GitHub Actions <actions@github.com>"

            # 推送
            git push

            echo "✅ 调优配置已提交到仓库"
          fi

      - name: 生成执行摘要
        if: always()
        run: |
          echo "## 📊 反馈分析执行摘要" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**执行时间**: $(date '+%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.check-feedback.outputs.found }}" == "true" ]; then
            echo "### ✅ 反馈文件" >> $GITHUB_STEP_SUMMARY
            echo "- 文件名: \`${{ steps.check-feedback.outputs.filename }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            if [ "${{ steps.check-tuning.outputs.tuning_generated }}" == "true" ]; then
              echo "### ✅ 调优配置已生成" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "新的参数配置将在今晚22:00的推荐生成中生效。" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "#### 配置内容" >> $GITHUB_STEP_SUMMARY
              echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
              cat skills/stock_daily_recommendation/tuning_config.json >> $GITHUB_STEP_SUMMARY
              echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            else
              echo "### ⚠️ 调优配置生成失败" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "请检查日志了解详情。" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "### ⚠️ 未找到反馈文件" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "请确保在18:00前提交反馈文件到 \`turning_feedback/\` 目录。" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "文件名格式: \`tuning_feedback_YYYYMMDD.csv\`" >> $GITHUB_STEP_SUMMARY
          fi

      - name: 上传分析日志
        if: always() && steps.check-feedback.outputs.found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: feedback-analysis-logs
          path: |
            skills/stock_daily_recommendation/logs/feedback_analysis.log
            skills/stock_daily_recommendation/tuning_config.json
          retention-days: 30
