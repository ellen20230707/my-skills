# GitHub Actions 自动化配置指南

本指南将帮助您将股票分析和推荐系统迁移到GitHub Actions，实现全自动化运行，不依赖本地电脑。

## 📋 目录

- [功能概述](#功能概述)
- [前置要求](#前置要求)
- [快速开始](#快速开始)
- [详细配置步骤](#详细配置步骤)
- [工作流说明](#工作流说明)
- [使用指南](#使用指南)
- [故障排除](#故障排除)

---

## 功能概述

### 自动化流程

```
每天21:00 (GMT+8) / 13:00 (UTC)
    ↓
【工作流1】每日股票数据更新
    • 自动获取当日交易数据
    • 更新所有股票CSV文件
    • 提交更新到仓库
    ↓
每天22:00 (GMT+8) / 14:00 (UTC)
    ↓
【工作流2】每日推荐生成
    • 运行技术分析
    • 生成推荐报告
    • 提交报告到仓库
    ↓
次日早上查看推荐报告
```

### 三个工作流

1. **每日股票数据更新** (`daily-data-update.yml`)
   - 定时执行：每天21:00 (GMT+8)
   - 手动触发：支持
   - 功能：更新股票数据

2. **每日推荐生成** (`daily-recommendation.yml`)
   - 定时执行：每天22:00 (GMT+8)
   - 自动触发：数据更新完成后
   - 手动触发：支持
   - 功能：生成推荐报告

3. **手动测试** (`manual-test.yml`)
   - 仅手动触发
   - 功能：测试完整流程

---

## 前置要求

### 1. GitHub账户
- 有效的GitHub账户
- 创建或使用现有的仓库

### 2. 本地准备
- 已运行过初始数据下载
- 拥有 `A股近10年日线数据/` 目录及CSV文件

### 3. Git配置
- 本地已安装Git
- 配置了Git用户信息

---

## 快速开始

### Step 1: 创建GitHub仓库

```bash
# 如果还没有仓库，在GitHub上创建新仓库
# 仓库名建议: my-skills 或 stock-analysis

# 在本地初始化Git（如果还没有）
cd /Users/ellen_li/2026projects/my-skills
git init
```

### Step 2: 添加远程仓库

```bash
# 添加远程仓库（替换为您的仓库地址）
git remote add origin https://github.com/YOUR_USERNAME/my-skills.git

# 或使用SSH
git remote add origin git@github.com:YOUR_USERNAME/my-skills.git
```

### Step 3: 准备.gitignore

```bash
# 创建.gitignore文件
cat > .gitignore << 'EOF'
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store

# 日志文件（可选，如果不想提交）
# logs/
# *.log

# 临时文件
_test_update/
EOF
```

### Step 4: 提交代码

```bash
# 添加所有文件（包括数据文件）
git add .

# 首次提交
git commit -m "🎉 初始提交：股票分析和推荐系统"

# 推送到GitHub
git push -u origin main
```

⚠️ **重要提示**：
- 确保 `A股近10年日线数据/` 目录被提交
- GitHub有文件大小限制（单个文件<100MB，仓库建议<1GB）
- 如果数据量太大，考虑使用Git LFS

---

## 详细配置步骤

### 1. 配置Git LFS（如果数据量大）

```bash
# 安装Git LFS
brew install git-lfs  # macOS
# apt install git-lfs  # Linux

# 初始化Git LFS
git lfs install

# 追踪大文件
git lfs track "A股近10年日线数据/*.csv"
git add .gitattributes
git commit -m "添加Git LFS配置"
```

### 2. 验证GitHub Actions文件

检查以下文件是否存在：

```bash
ls -la .github/workflows/

# 应该看到:
# daily-data-update.yml
# daily-recommendation.yml
# manual-test.yml
```

### 3. 推送到GitHub

```bash
git add .github/
git commit -m "添加GitHub Actions工作流配置"
git push
```

### 4. 在GitHub上验证

1. 访问您的仓库
2. 点击 **Actions** 标签
3. 应该能看到三个工作流：
   - 每日股票数据更新
   - 每日推荐生成
   - 手动测试完整流程

---

## 工作流说明

### 工作流1: 每日股票数据更新

**文件**: `.github/workflows/daily-data-update.yml`

**触发条件**:
- **定时**: 每天13:00 UTC (21:00 GMT+8)
- **手动**: 支持指定日期参数

**执行步骤**:
1. 检出代码
2. 设置Python环境
3. 安装依赖
4. 运行 `daily_data_updater.py`
5. 提交更新的CSV文件
6. 生成执行摘要

**输出**:
- 更新的股票CSV文件
- 执行日志

---

### 工作流2: 每日推荐生成

**文件**: `.github/workflows/daily-recommendation.yml`

**触发条件**:
- **定时**: 每天14:00 UTC (22:00 GMT+8)
- **自动**: 数据更新完成后
- **手动**: 支持立即触发

**执行步骤**:
1. 检出代码
2. 拉取最新数据更新
3. 设置Python环境
4. 运行 `daily_recommendation.py`
5. 提交推荐报告
6. 生成报告摘要
7. 上传报告为Artifact

**输出**:
- 推荐报告（TXT, HTML, JSON）
- 分析日志
- Artifact下载

---

### 工作流3: 手动测试

**文件**: `.github/workflows/manual-test.yml`

**触发条件**:
- **仅手动触发**

**参数**:
- `test_stock_limit`: 测试股票数量（默认100）
- `skip_data_update`: 是否跳过数据更新

**用途**:
- 验证配置正确性
- 测试完整流程
- 快速调试问题

---

## 使用指南

### 首次使用

#### 1. 手动触发测试

1. 访问GitHub仓库 → **Actions**
2. 选择 "手动测试完整流程"
3. 点击 **Run workflow**
4. 设置参数（可选）:
   - 测试股票数量: `100` (默认)
   - 跳过数据更新: `false` (默认)
5. 点击 **Run workflow** 开始测试

#### 2. 查看执行结果

- 在Actions页面查看运行状态
- 点击workflow查看详细日志
- 查看Summary了解执行摘要
- 下载Artifact获取生成的报告

#### 3. 验证成功标志

✅ 所有步骤都显示绿色对勾
✅ Summary显示报告已生成
✅ 仓库中出现新的推荐报告文件

---

### 日常使用

#### 查看每日推荐

**方式1: 直接访问仓库文件**

```
https://github.com/YOUR_USERNAME/my-skills/tree/main/skills/stock_daily_recommendation/recommendations
```

查看最新的：
- `recommendation_YYYYMMDD.txt` - 文本报告
- `recommendation_YYYYMMDD.html` - 网页报告（点击Raw或下载后在浏览器打开）
- `recommendation_YYYYMMDD.json` - JSON数据

**方式2: 下载Artifact**

1. Actions → 选择最新的"每日推荐生成"运行
2. 滚动到底部 → Artifacts
3. 下载 `recommendation-reports`
4. 解压查看报告

**方式3: 克隆仓库到本地**

```bash
git pull origin main
open skills/stock_daily_recommendation/recommendations/recommendation_*.html
```

---

#### 手动触发数据更新

如果某天自动执行失败，可以手动触发：

1. Actions → "每日股票数据更新"
2. Run workflow
3. 可选：指定更新日期（如 `2026-02-08`）
4. 点击运行

---

#### 手动触发推荐生成

如果需要立即生成推荐：

1. Actions → "每日推荐生成"
2. Run workflow
3. 点击运行

---

### 查看执行历史

1. 访问 **Actions** 标签
2. 选择具体的工作流
3. 查看所有历史运行记录
4. 点击查看详细日志和结果

---

## 时区说明

### GMT+8 到 UTC 转换

GitHub Actions使用UTC时间，需要转换：

| GMT+8 (北京时间) | UTC时间 | Cron表达式 |
|-----------------|---------|-----------|
| 21:00 | 13:00 | `0 13 * * *` |
| 22:00 | 14:00 | `0 14 * * *` |
| 09:00 | 01:00 | `0 1 * * *` |

**设置时区环境变量**:
```yaml
env:
  TZ: 'Asia/Shanghai'  # GMT+8
```

这确保日志和报告中的时间显示正确。

---

## 成本说明

### GitHub Actions免费额度

**公开仓库**:
- ✅ 完全免费
- ✅ 无限制的分钟数

**私有仓库（免费账户）**:
- 每月2000分钟免费
- 本项目预计每天使用约10-15分钟
- 一个月约300-450分钟，在免费额度内

### 存储空间

- GitHub仓库大小限制：建议<1GB
- Git LFS存储：免费账户1GB
- Artifact保留：默认90天，可调整

---

## 故障排除

### Q1: 工作流没有自动执行？

**可能原因**:
1. 仓库是私有的，且超出免费额度
2. GitHub Actions未启用
3. Cron时间配置错误

**解决方法**:
```bash
# 1. 检查Actions是否启用
# GitHub仓库 → Settings → Actions → General
# 确保 "Allow all actions and reusable workflows" 已选中

# 2. 检查仓库是否是公开的
# Settings → General → Danger Zone → Change visibility

# 3. 验证cron表达式
# 使用 https://crontab.guru/ 验证
```

---

### Q2: 数据更新失败？

**检查日志**:
1. Actions → 选择失败的运行
2. 展开 "更新股票数据" 步骤
3. 查看错误信息

**常见错误**:
- **网络错误**: Baostock服务器不可用，稍后重试
- **数据目录不存在**: 确保初始数据已提交到仓库
- **权限错误**: 检查GitHub token权限

---

### Q3: 推荐报告未生成？

**可能原因**:
- 数据更新失败
- 未发现符合条件的股票
- 脚本执行失败

**解决方法**:
```bash
# 1. 手动运行测试工作流
Actions → "手动测试完整流程" → Run workflow

# 2. 查看详细日志
展开所有步骤，查看具体错误信息

# 3. 本地测试
git pull
cd skills/stock_daily_recommendation
source ../../venv/bin/activate
python daily_recommendation.py
```

---

### Q4: Git LFS推送失败？

**解决方法**:
```bash
# 1. 检查LFS配置
git lfs env

# 2. 手动推送LFS文件
git lfs push origin main --all

# 3. 如果文件太大，考虑压缩或分批提交
```

---

### Q5: 如何修改执行时间？

编辑对应的工作流文件：

```bash
# 修改数据更新时间为20:00 GMT+8 (12:00 UTC)
vim .github/workflows/daily-data-update.yml

# 找到cron行，修改为:
    - cron: '0 12 * * *'  # 20:00 GMT+8

# 提交并推送
git add .github/workflows/
git commit -m "修改执行时间"
git push
```

---

### Q6: 如何暂停自动执行？

**方法1: 禁用工作流**
1. Actions → 选择工作流
2. 点击右上角 "..." → Disable workflow

**方法2: 修改触发条件**
```yaml
# 注释掉schedule部分
on:
  # schedule:
  #   - cron: '0 13 * * *'
  workflow_dispatch:  # 保留手动触发
```

---

## 高级配置

### 配置通知

#### 1. 邮件通知（默认）
GitHub会在工作流失败时发送邮件通知。

#### 2. 配置Slack通知（可选）

在工作流末尾添加：

```yaml
- name: Slack通知
  if: always()
  uses: 8398a7/action-slack@v3
  with:
    status: ${{ job.status }}
    text: '每日推荐报告生成完成'
    webhook_url: ${{ secrets.SLACK_WEBHOOK }}
  env:
    SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
```

---

### 优化性能

#### 1. 使用缓存加速

```yaml
- name: 缓存Python依赖
  uses: actions/cache@v3
  with:
    path: ~/.cache/pip
    key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
```

#### 2. 并行执行（如果有多个独立任务）

```yaml
jobs:
  update-data:
    runs-on: ubuntu-latest
    # ...

  analyze-data:
    runs-on: ubuntu-latest
    needs: update-data  # 依赖关系
    # ...
```

---

## 安全建议

### 1. 使用Secrets存储敏感信息

如果需要API密钥等敏感信息：

```bash
# GitHub仓库 → Settings → Secrets and variables → Actions
# 添加新的Repository secret
```

在工作流中使用：
```yaml
env:
  API_KEY: ${{ secrets.MY_API_KEY }}
```

### 2. 限制权限

```yaml
permissions:
  contents: write  # 仅授予必要的权限
  actions: read
```

### 3. 保护主分支

```bash
# Settings → Branches → Branch protection rules
# 添加规则，保护main分支
# 要求PR审核、状态检查等
```

---

## 总结

### ✅ 完成清单

- [ ] 创建GitHub仓库
- [ ] 添加远程仓库
- [ ] 提交代码和数据
- [ ] 验证工作流文件存在
- [ ] 运行手动测试工作流
- [ ] 验证测试成功
- [ ] 等待自动定时执行
- [ ] 查看每日推荐报告

### 📚 相关资源

- [GitHub Actions官方文档](https://docs.github.com/en/actions)
- [GitHub Actions定时任务](https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#schedule)
- [Git LFS文档](https://git-lfs.github.com/)
- [Cron表达式工具](https://crontab.guru/)

---

**现在您的股票分析系统已经迁移到云端，全自动运行！** ☁️🎉

每天22:00自动生成推荐，早上查看报告，不依赖本地电脑，随时随地都能访问！
